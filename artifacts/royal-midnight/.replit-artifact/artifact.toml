kind = "web"
previewPath = "/"
title = "Royal Midnight"
version = "1.0.0"
id = "artifacts/royal-midnight"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25848

[services.development]
run = "pnpm --filter @workspace/royal-midnight run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/royal-midnight", "run", "build" ]
publicDir = "artifacts/royal-midnight/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25848"
BASE_PATH = "/"
